kind = "web"
previewPath = "/"
title = "Agenvy Agency Website"
version = "1.0.0"
id = "artifacts/agenvy"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22106

[services.development]
run = "pnpm --filter @workspace/agenvy run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agenvy", "run", "build" ]
publicDir = "artifacts/agenvy/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22106"
BASE_PATH = "/"
